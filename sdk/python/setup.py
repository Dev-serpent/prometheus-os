from setuptools import setup, find_packages

setup(
    name="prometheus-sdk",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Prometheus OS Developers",
    description="Python SDK for Prometheus OS",
    python_requires=">=3.10",
)
